{{!-- todos --}}
{{!-- update users to posts --}}
{{!-- use bootstrap to style page --}}
{{!-- format html to look how we want homepage to be --}}

{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{user.name}}</h2>
  </div>
  <button>
    {{!-- set on click --}}
  </button>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}
